## Topics

### Initial Project Setup & Deployment
- **Goal**: Create a basic WYSIWYG markdown editor (scratch pad) and deploy it on Vercel.
- **Files affected**: `Initial commit` (likely a single file containing the app), `vercel.json`, `vite.config` (or similar), and a downgrade from Vite 8 to Vite 7 to fix a production build issue with MDXEditor (later removed).

### Editor Overhaul
- **Goal**: Replace the problematic MDXEditor component with a simpler textarea + `marked` preview to avoid build and stability issues.
- **Files affected**: Source files for the editor component, likely `src/App.tsx` or similar.

### Navigation & Storage Improvements
- **Goal**: Switch from query-parameter routing (`?name=`) to path-based routing (`/sheet-name`), prevent accidental auto-saving of unsaved markdown, add explicit Save button, and improve copy behavior using native selection.
- **Files affected**: `src/App.tsx`, `src/storage.ts`, possibly routing configuration.

### URL Generation Tools for Agents & Humans
- **Goal**: Provide both a user-facing URL generator and an API endpoint (`/api/url`) so agents can POST markdown and receive a sharable URL back. Also create a skill document for agents.
- **Files affected**: `src/url-generator.html` (later removed), `api/url.mjs`, `SKILL.md`, possibly `README.md`.

### API Fixes & Documentation
- **Goal**: Correct the API implementation: parse request body manually, make GET return JSON instead of a redirect, and update the agent skill doc accordingly.
- **Files affected**: `api/url.mjs`, `README.md`, `SKILL.md`.

### Short Shared URLs via Upstash Redis (PR #2)
- **Goal**: Replace the legacy long `?md=` URL scheme with short `?id=` URLs stored in Upstash Redis (7-day TTL). Add new GET endpoint (`/api/content`) to retrieve content by ID. Remove legacy local tooling.
- **Files affected**: `api/url.mjs`, `api/content.mjs` (new), `src/storage.ts`, `src/App.tsx`, `src/index.css`, `SKILL.md`, `README.md`, removal of `url-for-pad.js` and `url-generator.html`.

### My Sheets Panel (PR #3)
- **Goal**: Add a slide-over panel to list, open, and delete saved localStorage documents. Include title extraction, snippet preview, and timestamps.
- **Files affected**: `src/SheetsPanel.tsx` (new), `src/storage.ts`, `src/types.ts`, `src/App.tsx`, `src/index.css`.

## Releases / Versions

- No explicit version tags or releases were recorded.
- Note: Vite was downgraded from v8 to v7 to work around a build issue with Rolldown and MDXEditor.

## Unresolved

- No work-in-progress or unfinished features are mentioned in the commit messages or PR descriptions. The two pull requests were merged successfully. The project appears to be in a finished state for the given time period.