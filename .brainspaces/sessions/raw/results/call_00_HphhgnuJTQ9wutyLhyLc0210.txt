[{'Text': 'Edited scratch-pad/.issue-body.md:\n\n```diff\n@@ -1,0 +1,120 @@\n+## Problem\n+\n+Currently, when an agent POSTs markdown to `/api/url`, the returned URL contains the **entire markdown** as a query parameter:\n+\n+```\n+POST /api/url  { "md": "# Hello\\n\\nThis is **bold** text with lots of content..." }\n+→ { "url": "https://scratch-pad-beryl.vercel.app/?md=%23+Hello%0A%0AThis+is+**bold**..." }\n+```\n+\n+This is:\n+- **Wasteful** — the agent encodes, transmits, and may repeat the full markdown\n+- **Ugly** — the URL is a wall of encoded text, hard to share, read, or put in messages\n+- **Limited** — very long markdown can hit URL length limits (~2000 chars in some apps)\n+\n+## Solution\n+\n+Use **Upstash Redis** (available via Vercel Marketplace as the replacement for the sunset Vercel KV) to store the markdown server-side with a short ID. The API returns a tiny URL, and the frontend fetches the content on load.\n+\n+```\n+POST /api/url  { "md": "# Hello\\n\\nThis is **bold**..." }\n+→ { "url": "https://scratch-pad-beryl.vercel.app/?id=abc123xyz" }\n+```\n+\n+## Implementation Plan\n+\n+### Step 1: Add Upstash Redis dependency\n+\n+- Install `@upstash/redis` via npm\n+- Connect Upstash Redis to the Vercel project via Vercel Marketplace (one click in dashboard or `vercel install upstash`)\n+- This injects `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` env vars\n+- For local dev, run `vercel env pull` to sync into `.env.local`\n+\n+### Step 2: New API endpoint — `api/content.mjs`\n+\n+New serverless function to retrieve stored content:\n+\n+```\n+GET /api/content?id=abc123xyz\n+→ 200 { "md": "# Hello...", "name": "optional-sheet-name" }\n+→ 404 { "error": "Not found" }\n+```\n+\n+- Reads key `sp:abc123xyz` from Upstash Redis\n+- Returns the stored JSON payload `{ md, name, ts }`\n+- Supports CORS (same as existing `/api/url`)\n+\n+### Step 3: Modify `api/url.mjs` — store content, return short URL\n+\n+**POST /api/url** (changed behavior):\n+- Generate a short random ID using `crypto.randomUUID()` (native, no dep needed)\n+- Store `{ md, name, ts }` in Upstash Redis under key `sp:<id>` with a 7-day TTL\n+- Return `{ "url": "https://scratch-pad-beryl.vercel.app/?id=abc123xyz" }`\n+\n+**GET /api/url** (kept for agents limited to GET requests):\n+- Same behavior: store content, return short URL with `?id=`\n+\n+**Backward compat:** The endpoint signature is identical — agents send `{ md, name }` — only the returned URL changes.\n+\n+### Step 4: Modify frontend to load content via `?id=`\n+\n+**`src/storage.ts`** changes:\n+- Add `parseIdFromUrl()` function that reads `?id=` from URL\n+- Add `fetchContentById(id)` async function that calls `GET /api/content?id=...`\n+- Modify `getInitialState()` to:\n+  1. Check for `?id=` first → fetch content from API → return it (loadedFromUrl=true)\n+  2. Fall back to `?md=` for backward compat\n+  3. Fall back to localStorage / welcome markdown as today\n+\n+**`src/App.tsx`** changes:\n+- `getInitialState()` feeds into the initial state, so content flow just works\n+- Note: `getInitialState()` can no longer be synchronous — it needs to handle the async fetch. This is the trickiest part. Options:\n+  a. Make `App` show a loading state while fetching, OR\n+  b. Keep `getInitialState` sync for localStorage/`?md=` paths, and have a `useEffect` that fetches from `?id=` and sets state after mount\n+\n+### Step 5: Update documentation\n+\n+- **`SKILL.md`** — Update API response examples to show `?id=` URLs\n+- **`README.md`** — Same update, add Upstash Redis to tech stack table\n+- **`url-for-pad.js`** — Update to use API POST instead of local URL building\n+\n+## Files Changed\n+\n+| File | Action |\n+|------|--------|\n+| `package.json` | Add `@upstash/redis` dependency |\n+| `api/content.mjs` | **NEW** — GET endpoint to retrieve stored content |\n+| `api/url.mjs` | Store in Redis, return `?id=` URL instead of `?md=` |\n+| `src/storage.ts` | Add `?id=` parsing + API fetch for content loading |\n+| `src/App.tsx` | Handle async loading from `?id=` (useEffect approach) |\n+| `SKILL.md` | Update API docs to show `?id=` response format |\n+| `README.md` | Update tech stack and example URLs |\n+| `url-for-pad.js` | Update to use API flow |\n+| `vercel.json` | No change needed (already routes `/api/*`) |\n+\n+## Design Decisions\n+\n+1. **TTL of 7 days** — shared URLs are ephemeral. A week gives time to open and save to localStorage. Old links auto-expire.\n+\n+2. **Key prefix `sp:`** — namespaces all scratch-pad keys, avoiding collisions.\n+\n+3. **`crypto.randomUUID()` over nanoid** — zero-dependency, available in Node 19+. 36-char IDs. If shorter IDs are wanted later (e.g. 8-char base62), we can add `nanoid`.\n+\n+4. **`?md=` fallback retained** — existing bookmarks, shared URLs, and direct links still work. App checks `?id=` first, then `?md=`.\n+\n+5. **`@upstash/redis` over `@vercel/kv`** — Vercel KV was sunset. `@upstash/redis` is the current recommendation via Vercel Marketplace. HTTP-based, no persistent connections — perfect for serverless.\n+\n+6. **Copy URL button unchanged** — the frontend\'s "Copy URL" builds locally and keeps `?md=` for offline use. Agent API path is the priority.\n+\n+## Setup Required (One Time)\n+\n+In Vercel dashboard:\n+1. Go to Storage → Browse Marketplace → Upstash\n+2. Create a free Upstash Redis database (free tier: 10K commands/day, 256MB)\n+3. Auto-injects `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` env vars\n+4. Run `vercel env pull .env.local` to sync locally\n+\n+Alternative via CLI:\n+```\n+vercel install upstash\n+```\n\n```'}]